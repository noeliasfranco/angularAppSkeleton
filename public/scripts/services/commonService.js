define(['app','angular'], function (app, angular) {
    angular.module('Demo').service('CommonService', ['$http', function ($http) {

        this.getSocialMediaData = function () {
        };

    }]);
});
